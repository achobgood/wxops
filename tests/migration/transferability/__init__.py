"""Transferability Phase 2 documentation coverage tests.

These tests diff code constants (DecisionType, ALL_ADVISORY_PATTERNS,
DEFAULT_CONFIG, DEFAULT_AUTO_RULES, advisor routing table) against the
runbook docs in docs/runbooks/cucm-migration/. They are intentionally
strict — any drift fails the build.
"""
